---
interface Props {
    type?: 'horizontal' | 'vertical' | 'rectangle';
    class?: string;
}

const { type = 'horizontal', class: className = '' } = Astro.props;

const styles = {
    horizontal: 'w-full h-[100px] md:h-[150px]',
    vertical: 'w-full h-[600px]',
    rectangle: 'w-full aspect-square md:w-[300px] md:h-[250px]'
};
---

<div class={`relative bg-[#F0F0F0] rounded-xl border border-dashed border-gray-300 flex items-center justify-center overflow-hidden group ${styles[type]} ${className}`}>
    <!-- Label -->
    <span class="absolute top-2 right-2 text-[10px] font-bold text-gray-400 uppercase tracking-widest px-1.5 py-0.5 bg-white/50 rounded pointer-events-none">
        Reklam
    </span>
    
    <!-- Placeholder Content -->
    <div class="flex flex-col items-center gap-2 opacity-50 group-hover:opacity-70 transition-opacity">
        <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6 text-gray-400" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z" />
        </svg>
        <span class="text-xs font-black text-gray-400 uppercase tracking-widest text-center px-4">Reklam Alanı</span>
    </div>

    <!-- AdSense Script Placeholder -->
    <!-- Bu alan ileride gerçek reklam koduyla değiştirilecektir -->
</div>
